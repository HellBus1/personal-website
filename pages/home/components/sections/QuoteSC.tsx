import { Box, Typography } from "@mui/material";
import React, { ReactElement } from "react";

const QuoteSC = (): ReactElement => {
  return <Box>
    <Typography>
    </Typography>
  </Box>;
};

export default QuoteSC;